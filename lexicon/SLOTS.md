# Slots

NIKITA = Main Bot (Grok Bot), tertiary, git write.
GPT = ChatGPT slot, git write if connector.
GEM = Gemini slot, bundle only.
CLA = Claude slot (Anthropic), bundle only until connector.
GRK = extra Grok wheel seat, parked.
