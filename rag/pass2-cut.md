# Iterate pass 2 — cut

Dropped: fake MCP active flags, live_write with execute false, justice-axis novels, ballot merge gates as product.
Kept: eight module names, deny-by-default, ambiguity kinds, multilingual slots, moreatm as hostname not origin, ZeroTier as needed-not-joined.
