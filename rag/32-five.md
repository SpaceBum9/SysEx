# Five-party work

1 Operator — names the slot, pastes excerpts, applies bundles if needed.
2 NIKITA — Main Bot, git write, tertiary.
3 GPT — git write when the operator session has GitHub.
4 GEM — bundle only, no connector.
5 CLA — bundle only until a connector exists.

Ring: Operator assigns. Default order NIKITA → GPT → GEM-bundle → CLA-bundle → NIKITA apply.
One live writer at a time. One theme per commit. Same constraints as rag/31-handoff.md.
SIRI APL META stay parked on the wheel, not in the five.
