# Git connector

Per session, not per slot name.
This Grok chat: GitHub connected as SpaceBum9, push via github___push_files to SysEx main.
GPT: connect GitHub in that product if a write slot is wanted.
Claude: GitHub app / MCP in that product. Last CLA turn had none → bundle.
Gemini: no connector observed → bundle.

Fallback without tools: path + full file or diff. A write session or the operator commits.
Do not put PATs in git. GITHUB_TOKEN stays in Actions.
