# Embed pass (stub)

Pass = rebuild in-memory TF-IDF over rag/*.md via src/rag_vec.py.
CLI: python3 scripts/rag_vec.py <query>
Keyword sibling: python3 scripts/rag_query.py <query>
Not a stored index. No Pinecone, no OpenAI embeddings, no keys.
execute=false. See rag/30-vector.md.
