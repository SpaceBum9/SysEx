# SysEx modules

MCT = this kernel (config/runtime.json + health).
ATM = verbs initialize, sync, halt accepted; execute denied. Hostname moreatm.com when origin works.
RC = next_slot on wheel GPT GEM CLA GRK SIRI APL META. live_write false.
GARAS = deny if intent contains money, transfer, order, vendor, credential, destroy, execute; else observe_only allow.
BILO = lexicon in lexicon/BILO.md. Public core-label, not statute. Lyrics are mnemonic only.
ZT = ZeroTier overlay needed, not joined. Network id never committed. mcp_live false.
AMB = token kinds literal brand person_label health_claim technical_claim speculative. claims_external_state always false until evidence.
I18N = de en fr pl ko gs. Disclaimer language follows snapshot lang.
SoS = composition of the eight, not a ninth stack to load first.
