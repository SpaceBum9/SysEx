# Workflow executed 2026-09-19

1. Analyse — 14 MCT repos + SysEx tree + domain 503 + ambiguity literature.
2. Consolidate — eight modules, one kernel, old repos provenance only.
3. Iterate ×3
   - pass 1: inventory and roles
   - pass 2: drop mega-mode and fake-live connectors
   - pass 3: retrieval chunks (this directory)
4. Ingest — these files are the RAG pack. Point a retriever at `rag/`.
5. Next — outside git: Cloudflare origin for moreatm.com; local ZeroTier id.
