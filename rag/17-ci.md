# CI/CD

CI: GitHub Actions `.github/workflows/ci.yml` on push/PR to main.
Jobs: unittest discover + assert runtime execute=false vendor_live=false hold=true.
CD: none. No deploy to moreatm, no ZeroTier join, no vendor live, no secrets in workflow.
Compose commits trigger CI. CI passing is not execute.
