msg=$(cat "$1")
test ${#msg} -ge 8 || exit 1
echo "$msg" | grep -qi 'execute=true' && exit 1
actor_count=$(printf '%s\n' "$msg" | grep -Ec '^Actor: (GRK|GPT)$' || true)
test "$actor_count" -eq 1 || exit 1
exit 0
